{
  "name": "jarvis-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "wake:build": "zsh scripts/build-wake-helper.sh",
    "predev": "npm run wake:build",
    "dev": "tauri dev",
    "prebuild": "npm run wake:build",
    "build": "tauri build",
    "web:dev": "vite",
    "web:build": "tsc && vite build",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run web:build && cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}
